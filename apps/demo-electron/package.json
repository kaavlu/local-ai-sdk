{
  "name": "demo-electron",
  "private": true,
  "version": "0.0.1",
  "description": "Minimal Electron shell to exercise the Dyno TypeScript SDK (placeholder UI)",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json && electron ."
  },
  "dependencies": {
    "@dynosdk/ts": "0.0.1",
    "@google/genai": "^1.48.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^34.0.0",
    "typescript": "^5.7.3"
  }
}
